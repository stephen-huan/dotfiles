#!/bin/sh
# script to build the site locally

site_dir="book"

# build site
mdbook build --dest-dir "$site_dir" &&
# don't ignore files in .gitignore
prettier --write "$site_dir" --ignore-path .prettierignore
